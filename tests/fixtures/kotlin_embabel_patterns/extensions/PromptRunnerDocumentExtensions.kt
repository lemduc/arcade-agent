package com.embabel.patterns.extensions

/**
 * Minimized from embabel PromptRunnerDocumentExtensions:
 * extension-only file (no type declarations here).
 */
fun PromptRunner.withDocument(name: String): PromptRunner = this

fun PromptRunner.withPath(path: String): PromptRunner = this
